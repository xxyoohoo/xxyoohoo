//
#include <bits/stdc++.h>
using namespace std;
int is_even(int x)
{
    if(x==2) return 1;
    else if(x==3) return 0;
    else if (x==4) return 1;
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin >> n;
    cout << is_even(n);
    return 0;
}
